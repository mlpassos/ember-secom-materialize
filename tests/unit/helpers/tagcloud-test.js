import { tagcloud } from 'embeter-materialize/helpers/tagcloud';
import { module, test } from 'qunit';

module('Unit | Helper | tagcloud');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tagcloud([42]);
  assert.ok(result);
});
