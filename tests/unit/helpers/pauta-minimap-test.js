import { pautaMinimap } from 'embeter-materialize/helpers/pauta-minimap';
import { module, test } from 'qunit';

module('Unit | Helper | pauta minimap');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pautaMinimap([42]);
  assert.ok(result);
});
