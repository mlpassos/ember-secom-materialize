import { pautaClass } from 'embeter-materialize/helpers/pauta-class';
import { module, test } from 'qunit';

module('Unit | Helper | pauta class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pautaClass([42]);
  assert.ok(result);
});
