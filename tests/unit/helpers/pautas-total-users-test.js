import { pautasTotalUsers } from 'embeter-materialize/helpers/pautas-total-users';
import { module, test } from 'qunit';

module('Unit | Helper | pautas total users');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pautasTotalUsers([42]);
  assert.ok(result);
});
