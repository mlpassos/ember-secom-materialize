
import { lightboxUrl } from 'embeter-materialize/helpers/lightbox-url';
import { module, test } from 'qunit';

module('Unit | Helper | lightbox url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lightboxUrl([42]);
  assert.ok(result);
});

