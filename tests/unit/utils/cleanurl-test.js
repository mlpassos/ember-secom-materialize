import cleanurl from 'embeter-materialize/utils/cleanurl';
import { module, test } from 'qunit';

module('Unit | Utility | cleanurl');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cleanurl();
  assert.ok(result);
});
