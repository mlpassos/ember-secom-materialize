<div class="container">
  <div class="row">
    <div class="col s12 m12">
      {{#if session.isAuthenticated}}
          {{pautas-view model=model session=session lat=lat lng=lng zoom=zoom on-action="goPauta"}}
      {{else}}
          <h1 class="header">Acesso negado</h1>
          Você não tem permissão para acessar essa área
      {{/if}}  
    </div>    
  </div>
</div>
{{outlet}}