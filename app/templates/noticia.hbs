<div class="container">
	<div class="row">
	  <div class="col s12 m12">
	    <h2 class="header">{{props.title}}</h2>
	    <img src="{{content.thumbnail_images.full.url}}" class="img-responsive center-block" alt="...">
		<h3 class="text-left">{{content.title}}</h3>
		<p class="text-left">{{{content.content}}}</p>
	  </div>
	</div>
</div>
{{outlet}}